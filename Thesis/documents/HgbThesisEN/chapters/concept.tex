\chapter{Concept}
\label{cha:Concept}

In the previous chapter several approaches and the underlying principles for DR were analyzed in detail. To evaluate the aforementioned methodologies a evaluation program needs to be developed. This implementation will be created for a HMD, as it provides the user with a lot more freedom of movement and interactions compared to a phone camera

In this chapter, the design decisions for the program are elaborated upon in further detail.

\section{Requirements}
The focus of this thesis lies on the evaluation of the inpainting methodologies and how they perform in a DR environment in terms of performance, quality and reduction of visual clutter.
To achieve this there are several surrounding factors, which need to be addressed:

\begin{itemize}
\item The user should easily and intuitively be able to select the area to apply the inpainting to by themselves.

\item It should be simple to switch inpainting methodologies on the fly, to compare them on the same mask in as similar of an environment as possible

\item The evaluations of quality and clutter reduction should be calculated in the background as to not affect the inpainting performance marginally\\

\item The architecture should be modular and easy to extend, in case other methodologies need to be analyzed.

\end{itemize}

\section{Workflow}

\begin{figure}[htb!]
  \begin{center}
    \includegraphics[width=1\textwidth]{OverlayProcess.png}
    \caption{Flowchart visualization of the prototypes' workflow}
    \label{fig:OverlayPipeline}
  \end{center}
\end{figure}

The entire workflow of the prototype can easily be visualized in a flowchart (see \ref{fig:OverlayPipeline}). As a baseline, a regular AR application in a Video-Based HMD always shows the camera feed in the background. This is most commonly achieved by using the camera feed as the texture for the skybox.

From this state, the user can start the workflow by selecting an area in their viewport, which is then interpreted as the mask for inpainting.

\subsection{User Input}
\label{cha:UserInput}
One of the considerations during the conceptualization of the implementation was the ease and simplification of the area selection.

By using Hand-Tracking to select the area, the area selection should be easy to pick up and use. Whilst hand gestures are easier to explain to users, it also avoids the requirement of having controllers to use the prototype,
which would of course add additional visual clutter to the users view.

\begin{figure}[htb!]
  \begin{center}
    \includegraphics[width=1\textwidth]{AreaSelectionGesture.PNG}
    \caption{Area selection using hand gestures}
    \label{fig:AreaGesture}
  \end{center}
\end{figure}

By placing their hands on the corners of the area they want to mask the gesture should remain rather intuitive and easy to understand.
Since the selection should only be triggered by a certain gesture instead of permanently being active, the L-gesture as seen in \ref{fig:AreaGesture}, may allow the user to interpret their index finger and thumb as the contours of the mask
The selected area can then be kept until the user decides to select a new area to overlay.

After the selection of the area to inpaint is finished, the constraints of the rectangle are being transformed into concrete pixel coordinates on the inpainting overlays image. In \ref{fig:OverlayPipeline}, the mask being overlaid over the image can be seen.

\subsection{Inpainting Design Decisions}
\label{cha:InpaintingDesign}
As such a black box may prove distracting to the user, especially in brighter areas, this mask needs to be filled with plausible substitute pixels, which can be achieved through inpainting. Since these inpainting algorithms were designed for static images, there are a few design adjustments and optimizations to make them plausible for real time use.

Whilst most of the methodologies analyzed in this thesis have vastly differing implementations, all of them can be boiled down to receiving an array of source pixels and a mask in the form of a list of the pixels to inpaint.

Since the methodologies analyzed in this thesis have only been conceptualized for static images, there are several optimizations to improve the performance for a real time use.
Most of the methodologies need to iterate over a large amount of pixels. To even make inpainting in a reasonable timespan plausible, an easy solution is to downscale the source image for the inpainting and upscaling it again afterwards. Whilst this reduces
the quality of the inpainting methodologies, it should in return end up improving the performance of the inpainting significantly.

Even with this reduction of the image size, the NLTM algorithm is still rather performance-heavy for its canidate-search. For this, the algorithm iterates over every pixel and calculates the similarity to the target pixel patch.
Whilst this is intuitive and great if the focus is on quality rather than performance, it makes the algorithm infeasable for real time use.

\begin{figure}[htb!]
  \begin{center}
    \includegraphics[width=1\textwidth]{NLTMCacheCanidates.PNG}
    \caption{Prior frames canidates are used to reduce the canidate search area decisively}
    \label{fig:NLTMCacheCanidates}
  \end{center}
\end{figure}

In theory this canidate patch search over the entire image should only be necessary once though. Since the viewport can only change up to a certain degree per frame, instead of always searching the entire image again, the initial canidates can be reused.
This would mean, NLTM would use an approach similar to the ELTM algorithm or the PatchMatch algorithm of Barnes, Shechtman, Finkelstein and Goldman from 2009 (see \cite{BarnesConnelly2009Parc}), albeit looking for canidates in the surrounding areas of the prior frames canidates instead of right next to the target pixel patch. As one can see in \ref{fig:NLTMCacheCanidates}, this approach reduces the search area for inpainting canidates decisively and should
provide a noticeable performance improvement with little to no quality loss for all subsequent frames after the initial full image search frame.

\section{Evaluation Approach}

\subsubsection{Measuring Quality for Inpainting}



Use InPainting Quality Benchmarking, Part with Quality Benchmarking without knowing what it should look like,
also mention that as a small issue in the evaluation

\subsubsection{Measuring Visual Clutter}
See Paper Measuring Visual Clutter

\subsubsection{How Performance is measured}
Use Clock/Timer for the Inpainting Method from Call to Return + FrameTime + FrameRate